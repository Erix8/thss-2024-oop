#pragma once

// Runs test cases for every overloaded operator (at least five cases per
// operator, covering five different situations such as pure imaginary).
// Returns the number of failed checks (0 means all passed).
int runAllComplexTests();
